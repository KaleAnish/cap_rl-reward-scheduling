# CapRL — Reward Scheduling & Algorithm Comparison

This repo explores multi-objective, scheduled rewards (QA, CLIP, length, coverage) in CapRL
and compares GRPO, REINFORCE/SCST, and PPO-KL under identical reward settings.

## Setup
- Python 3.10+
- Install deps: `pip install -r requirements.txt`

## Run (placeholder)
- Training entrypoint: `python -m src.train`
